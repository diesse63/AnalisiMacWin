{
  "name": "gestione-analisi-mediche",
  "version": "1.0.0",
  "description": "Gestione referti medici - applicazione portable",
  "author": "Diesse63",
  "main": "src/main.js",
  "scripts": {
    "dev": "electron .",
    "build-backend-win": "cd backend && build_exe.bat",
    "build-backend-unix": "cd backend && ./build_exe.sh",
    "build-and-dist-win": "npm run build-backend-win && npm run dist",
    "dist": "electron-builder --publish never"
  },
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-builder": "^24.9.0",
    "electron-publisher-never": "^1.0.0"
  },
  "dependencies": {
    "electron-context-menu": "^4.1.1"
  },
  "build": {
    "appId": "com.gestioneanalisi.app",
    "productName": "Gestione Analisi Portable",
    "directories": {
      "output": "dist"
    },
    "files": [
      "src/**/*",
      "index.html"
    ],
    "extraResources": [
      {
        "from": "backend/dist",
        "to": "backend/dist",
        "filter": ["**/*"]
      },
      {
        "from": "backend",
        "to": "backend",
        "filter": ["api.exe"]
      }
    ],
    "publish": "never",
    "win": {
      "target": ["portable"]
    },
    "compression": "store",
    "portable": {
      "artifactName": "${productName}_Portable_Win_${version}.${ext}",
      "requestExecutionLevel": "user"
    },
    "mac": {
      "target": ["dmg", "zip"],
      "artifactName": "${productName}_Mac_${arch}_${version}.${ext}"
    }
  }
}